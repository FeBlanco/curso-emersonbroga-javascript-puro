// Funções Condicionais 

// !== diferente

function checkValues(numero1, numero2) {
    if (numero1 !== numero2) {
        return 'Falso';
    } 
    return 'Verdadeiro' 
}

const result = checkValues(10, 9);
console.log(result);