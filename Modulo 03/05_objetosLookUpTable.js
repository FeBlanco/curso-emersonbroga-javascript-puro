// objetos  Look up table tabela de indice e valores que serve para buscar objetos

const items = {
    fruit: 'laranja',
    car: 'fusca',
    instrument: 'violao',
};

const item =  'fruit';
const result = items[item];

console.log(result);